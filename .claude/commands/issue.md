Show the full detail of a specific GitHub issue.

The user will provide the issue number as an argument (e.g. `/issue 3`).

Steps:
1. Run `gh issue view [N]` where N is the issue number provided.
2. Display: title, status, labels, body, and all comments.
3. If no number is provided, ask the user which issue they want to see.
